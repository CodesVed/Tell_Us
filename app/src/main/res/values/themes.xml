<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FeedbackCollectorSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FeedbackCollectorSystem" parent="Base.Theme.FeedbackCollectorSystem" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/blue_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/feedback_app_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.FeedbackCollectorSystem</item>
    </style>

</resources>